buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
# Target TGF: 90.0
#  >methods=5
#  >complexity=40
#  >construction=9.0
#  >dependency=4.0
 - target:
   - dir: target/classes
     file: com/open/boot/core/ResultGenerator.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/ResultGenerator.java
   - dir: target/classes
     file: com/open/boot/core/Result.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/Result.java
# Target TGF: 526.5
#  >methods=6
#  >complexity=94
#  >construction=9.25
#  >dependency=1.5
 - target:
   - dir: target/classes
     file: com/open/boot/core/interceptor/CameHumpInterceptor.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/interceptor/CameHumpInterceptor.java
# Target TGF: 1410.75
#  >methods=5
#  >complexity=461
#  >construction=29.25
#  >dependency=1.58333333333
 - target:
   - dir: target/classes
     file: com/open/boot/core/utils/FileManageUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/utils/FileManageUtil.java
   - dir: target/classes
     file: com/open/boot/core/utils/CommonUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/utils/CommonUtil.java
# Target TGF: 262.8
#  >methods=4
#  >complexity=127
#  >construction=20.25
#  >dependency=1.7
 - target:
   - dir: target/classes
     file: com/open/boot/core/utils/ExportTextUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/utils/ExportTextUtil.java
# Target TGF: 3762.0
#  >methods=15
#  >complexity=418
#  >construction=117.0
#  >dependency=13.0
 - target:
   - dir: target/classes
     file: com/open/boot/core/utils/CommonUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/utils/CommonUtil.java
# Target TGF: 336.910714286
#  >methods=9
#  >complexity=293
#  >construction=22.5
#  >dependency=1.80952380952
 - target:
   - dir: target/classes
     file: com/open/boot/pdf/PdfTEmplate.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/pdf/PdfTEmplate.java
   - dir: target/classes
     file: com/open/boot/common/enumset/Constant$PdfEnum.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/common/enumset/Constant.java
   - dir: target/classes
     file: com/open/boot/pdf/model/Rock.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/pdf/model/Rock.java
# Target TGF: 192.5
#  >methods=18
#  >complexity=200
#  >construction=15.25
#  >dependency=16.0
 - target:
   - dir: target/classes
     file: com/open/boot/core/AbstractService.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/AbstractService.java
   - dir: target/classes
     file: com/open/boot/core/Service.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/Service.java
   - dir: target/classes
     file: com/open/boot/core/Mapper.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/Mapper.java
   - dir: target/classes
     file: com/open/boot/core/ServiceException.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/ServiceException.java
# Target TGF: 1250.25
#  >methods=6
#  >complexity=356
#  >construction=38.25
#  >dependency=2.83333333333
 - target:
   - dir: target/classes
     file: com/open/boot/core/utils/DigestUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/utils/DigestUtil.java
# Target TGF: 89.28125
#  >methods=5
#  >complexity=191
#  >construction=2.25
#  >dependency=2.125
 - target:
   - dir: target/classes
     file: com/open/boot/user/service/impl/AliyunDysmsServicelmp.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/user/service/impl/AliyunDysmsServicelmp.java
   - dir: target/classes
     file: com/open/boot/user/service/IAliyunDysmsService.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/user/service/IAliyunDysmsService.java
   - dir: target/classes
     file: com/open/boot/user/model/AliyunSendSmsRequestBo.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/user/model/AliyunSendSmsRequestBo.java
   - dir: target/classes
     file: com/open/boot/user/model/AliyunDysmsTemplateEnum.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/user/model/AliyunDysmsTemplateEnum.java
# Target TGF: 518.083333333
#  >methods=59
#  >complexity=1412
#  >construction=58.0
#  >dependency=20.9166666667
 - target:
   - dir: target/classes
     file: com/open/boot/core/redis/RedisUtil.class
     sourceDir: src/main/java
     sourceFile: com/open/boot/core/redis/RedisUtil.java
